#!/bin/sh
srcdir=`dirname "$0"`
git -C ${srcdir} pull

${srcdir}/main.py >.travis.yml

DATE=`date -u +%Y%m%d`
git add .travis.yml
git commit -m "Update ${DATE}" -m "Automatically generated by meson-corpus-test-update"
git push origin master
